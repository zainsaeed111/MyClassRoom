ktor:
  application:
    modules:
      - com.myclassroom.ApplicationKt.module
  deployment:
    port: ${PORT:8080}
jwt:
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
